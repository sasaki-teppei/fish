<%#= @topics.all.sum(:point) %>
<%#= @replies.all.sum(:point) %>
<%# array = [] %>
<%# @replies.all.each do |reply| %>
<%# @topics.all.each do |topic| %>
  <%# array = topic.point, reply.point %>
<%# end %>
<%# end %>
<%#= array %>
<h>総売上ポイント</h><br>
<%# @sum = @topics.all.sum(:price) ++ @replies.all.sum(:price) %><br>
<%#= @sum %>
<% if @company.point.present? %>
  <%= @company.point %>
<% else %>
  <h>0 pt</h>
<% end %>
<%= form_tag("/companies/transfer") do%>
  <%= submit_tag"振り込み依頼をする" %>
<% end %>