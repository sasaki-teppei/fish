<div class="show-wrapper text-center">
    <p>
      <%= current_company.company_name %>
    </p>
    <p>
      <%= current_company.address %>
    </p>
    <p>
      <%= current_company.phone_number %>
    </p>
  <% current_company.topics.each do |topic| %>
    <p>
      <%= topic.content %><br>
      <%= topic.price %>
    </p>
    <p>
      <%= image_tag topic.image_name.url %>
    </p>
    <p>
      <%= topic.created_at %>
    </p>
  <% end %>
</div>